<main>
  <div class="l-container">
    {{> styleguide-template-general/c-page-component-header}}
    <!-- end c-page header -->
    <div class="site-layout site-layout--primary">
      <div class="site-layout__stage" id="siteStageContainer">
        <main class="site-stage">
          <div class="container">
            <section class="template template--fullwidth">
              <div class="template__primary">
                <div class="flex-space-apart flex-layout-2col">
                  <!-- colors -->
                  {{> styles/colors}}
                  <!-- end colors -->
                </div>
              </div>
            </section>
          </div>
        </main>
      </div>
    </div>
  </div>
</main>